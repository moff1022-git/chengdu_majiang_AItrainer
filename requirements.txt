# Runtime
pygame>=2.5

# Dev / test
pytest>=7.0

# Optional: vector observations in training.env.encode_obs_vector
# numpy>=1.24

# Optional: Gym branding only — ChengduMahjongEnv is duck-typed, not required
# gymnasium>=0.29
