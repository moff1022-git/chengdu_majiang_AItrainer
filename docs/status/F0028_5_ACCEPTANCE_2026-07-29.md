# F0028-5 决策审计与确定性回放验收 — 2026-07-29

## 结论

**通过，F0028-5 状态 `Approved → Done`。**

已实现 private Audit v1、canonical SHA-256 记录链、state/view/config hash、DecisionTrace v2 与认知/RNG 快照、严格验证器，以及按审计 PlayerView 序列进行的 humanlike 策略复演。旧 M10 steps/ReplaySession 保持兼容。

## 交付

| 能力 | 路径 |
|------|------|
| Audit v1 writer / reader / verifier | `engine/audit.py` |
| humanlike 策略复演 | `players/humanlike/audit_replay.py` |
| 决策前后 state hash 与实际 view/legal 接线 | `engine/orchestrator.py` |
| canonical public event token 修正 | `players/humanlike/memory.py` |
| 篡改/泄漏/回放/双跑测试 | `tests/humanlike_v2/test_audit_replay.py` |

## 自动验收

| 门禁 | 结果 |
|------|------|
| F0028-5 定向 | **19 passed** |
| 全量 | **338 passed / 1 skipped**（29.55s） |
| compileall / diff check | 通过 |
| 2 人审计批跑 | 20 局、1389 决策，20/20 链验证与策略复演通过 |
| 3 人审计批跑 | 20 局、3547 决策，20/20 链验证与策略复演通过 |
| 4 人审计批跑 | 20 局、4358 决策，20/20 链验证与策略复演通过 |
| 合计 | 60 局、9294 决策，零 replay mismatch |
| 同 game_id 双跑 | 完整 `record_hash` 序列一致 |
| writer 性能 | 20 局未记录 6.32s；audit+steps 8.69s；**1.375×**（≤1.5×） |
| verifier 吞吐 | 4251 records / 0.676s = **6288.3 records/s**（≥1000） |

性能环境：macOS arm64、Python 3.12.13、项目 `.venv`。

## 安全与完整性结果

- 修改记录内容后不重算 hash 会触发 record hash mismatch；截断文件在 strict 模式失败。
- PlayerView 注入他家 concealed hand 或墙顺序会被 writer/verifier 拒绝。
- selected action 必须属于 legal actions；humanlike trace 必须为 v2 且内外动作一致。
- RNG index 按 seat 不回退；`rng_used=false` 时 before/after 必须相等。
- hash 不含随机 request_id、时间戳、文件路径或对象 repr；同局独立运行链一致。
- 冻结 Mapping 的最近公开事件改为递归 thaw 后 canonical JSON，消除落盘前后 token hash 漂移。

## 兼容与差异

- `save_every_decision=True` 并行生成 `.steps.jsonl` 与 `.audit.jsonl`。
- StepRecorder 周期快照改为动作应用后的状态；ReplaySession 仍按 snapshot 行读取，旧测试通过。
- Audit 含该 seat 可见的本家暗手，属于 private diagnostic，不是公开回放格式。
- 本切片复演策略输入序列；完整规则引擎事件重演仍依赖 M10 steps，符合 Out of Scope。
- APP 0.2.1 / state 5 / PlayerView 2 / persistence 1 / wire 1 均未升级。

## 临时证据

`/tmp/f0028_5_batch_*`、`/tmp/f0028_5_perf_audit/`、`/tmp/f0028_5_audit/`。这些目录不入库，不作为跨机状态源。
