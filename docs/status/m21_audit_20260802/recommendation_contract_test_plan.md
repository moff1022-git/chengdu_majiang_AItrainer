# 当前推荐算法合同测试计划

用于替代已废弃的 F0011 集成合同，不改变业务代码。

## 必测合同

1. `rule_ai` 为默认算法；
2. `rule_ai_plus` 可从设置窗口选择并持久化；
3. `humanlike_v2` 可选择并持久化 13 种人格预设；
4. 保存后从下一次 discard 请求生效；
5. 输出动作必须属于 engine 提供的合法动作集合；
6. hints 包含算法和人格字段；
7. 不调用 `strategy.rank_discards/F0011`；
8. 运行时配置根字段保持 schema 严格兼容。

## 当前状态

设计已登记，尚未新增测试代码；旧 `tests/test_f0011_integrated.py` 保留为历史证据并从当前基线测试命令排除。
