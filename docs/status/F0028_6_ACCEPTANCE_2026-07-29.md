# F0028-6 训练契约 v2 验收报告

| 项 | 结果 |
|---|---|
| 日期 | 2026-07-29 |
| 规格 | `docs/features/F0028_6_training_contract.md` |
| 状态 | **Pass / Done** |

## 自动验收

- 固定 codec：635 项全量唯一 roundtrip；495 个同门换三张组合稳定。
- Observation v2：2/3/4 人固定块与 635 mask；flat 长度 921；无 `physical_hand`。
- 非法动作：raise 保持状态不变；terminate 返回配置 penalty 并结束。
- 奖励：默认 shaping 关闭；开启时与 base、true score 分离报告。
- 定向测试：18 passed。
- 全量回归：347 passed / 1 skipped（29.04s）。
- 批跑：2/3/4 人各 50 局，共 150 局；全部自然闭环，非法动作 0；共 10,950 个 learner 决策观测样本。
- Observation v2 + mask 构建 p95：0.4917 ms，低于 5 ms 门禁。
- 30 局 v1/v2 对照：v1 1.8333s，v2 1.9886s，v2/v1=1.0847×，低于 1.5×门禁。
- `compileall training` 通过。

## 范围与兼容

- v1 仍为默认，整数仍解释为当前 legal list 下标。
- v2 显式 opt-in，整数使用固定 codec；响应 HU 的实体牌上下文在 env 边界恢复，不进入固定索引。
- 未修改 engine 规则、PlayerView builder、humanlike 策略或 GUI；未引入 Gym/RL 框架依赖。

## 结论

F0028-6 规格验收项全部通过，状态由 `Approved` 更新为 `Done`。F0028-1–6 工程切片全部完成。
