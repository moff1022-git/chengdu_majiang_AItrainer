# F0028-2 验收报告（2026-07-28）

## 结论

F0028-2 全部功能、兼容与性能门禁通过，状态可由 `Approved` 更新为 `Done`。

## 版本锚点

| 版本线 | 结果 |
|--------|------|
| GameState schema | 5；reader 支持 1–5 |
| persistence format | 1 |
| Human wire protocol | 1 |
| PlayerView | 2 |

## 功能验收

- PhysicalTile ID 0–107 双向换算、108 唯一、每 face 四张。
- wall / hand / meld / unclaimed discard / transit / winning 六类所有权区域并集为 108 且互斥。
- schema 1–4 确定性迁移，坏档明确失败，schema 5 round-trip 保持 ID。
- 原子事件边界执行实体、引用、副露、状态和账本断言。
- PlayerView v2 显式白名单，GP-021 八项 hidden/partial/exact 覆盖。
- 哨兵私有字段、他家手牌、wall ID、transit、pending claims 和 oracle 均不泄漏。
- TrainingTruth 与普通 Observation 分离；Human wire/UI 不再接收 oracle。

## 自动测试

- F0028-2 + state/view/persistence 定向：48 passed（中途门禁）。
- 最终全量：**308 passed / 1 skipped in 28.26s**。
- skip 为既有 macOS Tk 原生安全门禁，与 F0028-2 无关。
- 额外固定批量：2/3/4 人各 20 局，共 **60 局**，全部完成且事件守恒断言无失败。
- `compileall`：通过。

## 性能门禁

环境：`macOS-26.5.2-arm64-arm-64bit`；单次本机抽样，数值仅作为本切片回归锚点。

| 项 | 对照 | F0028-2 | 变化 | 门禁 |
|----|------|---------|------|------|
| PlayerView 构建，3000 次中位型循环 | 旧式全状态 deepcopy/filter 56.15 µs | 白名单 v2 42.81 µs | **-23.8%** | 不恶化超过 25%：通过 |
| 四家 RuleAI，固定 12 局 | 关闭断言 1.6034s | 开启断言 1.5687s | **-2.2%**（采样噪声范围） | 不恶化超过 20%：通过 |

## 已知行为变化

- schema 5 新 writer 不再输出 face-only `wall` / `players[*].hand` 权威字段；旧档仅在内存迁移，显式重存才写 schema 5。
- Human/AI 座位窗口继续看到兼容牌面字段；自身实体 ID 只存在强类型 PlayerView v2，不进入 legacy dict。
- F0010 局中预测继续运行，但普通 Observation 不再携带真实对手手牌，因此局中 oracle 准确率不再计算；离线评估保留。
