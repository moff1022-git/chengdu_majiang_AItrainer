# F0029 Humanlike v2 UI 与全参数设置验收

| 项 | 结果 |
|---|---|
| 日期 | 2026-07-29 |
| 规格 | `docs/features/F0029_humanlike_v2_ui_settings.md` |
| 状态 | **Pass / Done** |

## 实现结果

- 大厅新增 Humanlike AI v2 关/开/混合状态、一键切换和参数入口。
- 主桌控制面板新增下局 Humanlike 开关及参数入口。
- AI 座位窗新增逐座 `人类v2` 开关和参数入口；Human 座不显示 AI 控件。
- 独立 Tk 参数窗口展示并编辑完整 JSON：GP-001–027、S0–S3 profile、版本、ruleset 与 seed。
- 保存复用 F0028 严格 validator，采用同目录临时文件、fsync、原子替换并生成 `.bak`。
- 当前局玩家实例不热更新，开关与参数保存均下局生效。

## 验收结果

- 定向 UI/配置服务测试：6 passed。
- 全量回归：352 passed / 1 skipped（30.57s）。
- compileall：通过。
- `git diff --check`：通过。
- 真实 Tk 窗口启动：`settings-window-ok`。
- 大厅既有 640×400、885×498、1100×720 离屏布局测试通过；Humanlike 控件在适用布局可点击。

## 已知限制

- 参数窗口采用完整、可搜索的 JSON 表达，不提供每个叶子字段独立表单控件；非法或锁定字段由 validator 精确拒绝。
- 运行中不热替换 AI 或认知状态，这是审计哈希稳定性的设计约束。

## 结论

F0029 自动验收通过，状态更新为 `Done`。后续建议执行用户可见的大厅、主桌和 AI 座位窗口人工点击验收。
