# F0028-3 验收报告 — 2026-07-29

## 结论

F0028-3 已按批准规格完成，状态 `Done`。`humanlike_v2` 已作为选配玩家注册，只使用 PlayerView v2、ActionRequest、批准配置和自身 RoundRuntime；旧 AI 默认行为未改变。

## 交付范围

- 上下文与隔离：`players/humanlike/view.py`
- 公开 belief：`players/humanlike/belief.py`
- 手牌特征：`players/humanlike/hand_analyzer.py`
- 无状态计划：`players/humanlike/plan.py`
- mandatory/ordinary 候选：`players/humanlike/candidates.py`
- 确定性 Q(action) 与 trace：`players/humanlike/evaluator.py`
- 玩家生命周期：`players/humanlike/player.py`
- 接入：`players/registry.py`、`configs/strategies/presets.json`、`engine/orchestrator.py`

## 自动化结果

| 门禁 | 结果 |
|------|------|
| 全量 pytest | 321 passed / 1 skipped in 29.24s |
| compileall | 通过 |
| 2/3/4 人批跑 | 各 50 局，共 150 局 |
| 批跑决策 | 23392 次 |
| 策略崩溃 / 非法动作 | 0 / 0 |
| 跨 hash seed | PYTHONHASHSEED 1 与 777 摘要相同 |
| 动作摘要 SHA-256 | `e541199f51e9c1c3c2702555c3d7b606204176acaab6fe79d51ab1c0b9b53d2b` |
| 单决策性能 | p95 2.87 ms，最大抽样 16.933 ms |
| 相对 RuleAI | 10 局四人批跑 2.222×（门禁 ≤3×） |

性能环境：macOS arm64，Python 3.12.13。最终 150 局批跑耗时 33.955s。

## 信息隔离

- orchestrator 仅向 legacy `RuleAIPlayer` 注入 `_engine_state`；humanlike_v2 实例无该属性。
- AST/源码测试禁止策略模块导入 `training.oracle` 或引用 GameState 全知入口。
- frozen PlayerView 的副露 mapping 显式复制后才进入牌型解析。
- 被认领弃牌在 visible count 中去重，避免与公开副露重复计算。
- trace 仅含公开 belief 摘要、候选分量与所选动作，`rng_used=false`。

## 实现差异

- wire 1 的 Observation 仍携带 legacy mapping；策略入口按 PlayerView v2 白名单重建冻结对象，没有协议升级。
- 原计划多个测试文件合并为两个聚合测试文件，验收覆盖不变。
- 未调用现有 F0010/F0011 全知入口，采用纯可见输入启发式；后续可在保持接口不变的前提下替换。

## 未覆盖范围

记忆、注意力、人格动态、满意停止、有界噪声和思考时长属于 F0028-4；持久化审计属于 F0028-5；训练动作空间与奖励属于 F0028-6。
