# Task 19 进度 Schema

Markdown 固定章节和 32 列明细表以 `task19_progress_tracker.md` 为准。门禁共16项；PASS 计分、NOT_REQUIRED 不进分母，其余不计分；未审计最高93.75%，AUDITED固定100.00%。AC格式 `通过数/总数`，defects格式 `P0/P1/Other`，证据多值用 `<br>`。

权威 writer 仅 Terminal 0。开发终端只生成 CSV/JSON delta，不编辑 Markdown。
