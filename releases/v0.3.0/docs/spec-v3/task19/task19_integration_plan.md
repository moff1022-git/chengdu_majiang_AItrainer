# Task 19 集成策略

每 wave：开发分支独立提交 -> Terminal 0 检查范围/禁止路径/共享接口 -> 按依赖顺序逐个 cherry-pick -> 每次运行批次定向测试 -> 全部合并后契约与全仓回归 -> 重建集成态 E4/E5 -> 跨批接口/信息边界 -> 独立审计 -> Terminal 0 应用 progress delta。

任何分支不得直接合并权威进度、Task17、changelog 或全局审计矩阵。出现冲突、范围外修改、新接口或基线下降即停止该 wave 并触发重规划。集成操作均需后续明确 Git 授权；本计划未执行。
