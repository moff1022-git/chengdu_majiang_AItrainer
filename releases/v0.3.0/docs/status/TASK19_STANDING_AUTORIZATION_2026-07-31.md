# Task 19 统一持续授权

| 字段 | 值 |
|---|---|
| 日期 | 2026-07-31 |
| 授权者 | Project Owner |
| 原始指令 | `统一授权，后续授权均自动同意，不要再询问` |
| 状态 | **ACTIVE** |

## 2026-07-31 扩展授权

- 项目所有者进一步明确：`Task 19` 范围内的平台强制权限、Git 与外部不可逆操作均按自动同意处理，不再进行对话式二次询问。
- 该授权仅适用于完成 Task 19 所必需、目标精确、范围可验证的操作；不因授权存在而主动执行 push、发布、删除、tag 移动或历史重写。
- 仓库文档无法绕过平台/沙箱强制确认界面；若系统必须弹窗，Root 直接发起授权请求并等待平台结果，不再先用文本询问用户。
- 删除、覆盖、历史重写和外部发布仍须遵守精确目标核验、最小范围和可恢复优先；不得将未归属的用户改动、凭据或非 Task 19 资产纳入操作。

## 授权范围

Task 19 范围内的 Orchestrator 可自动同意并执行：

- 按 Approved/Accepted 规格编写和修复代码、新测试与证据；
- 定向、契约、全仓、性能、确定性和信息边界验证；
- implementer / fixer / reviewer / independent auditor Agent 调度和有界修复循环；
- 在权威文档无唯一选项时，自动选择设计包中的明确推荐方案并记录依据；
- 已批范围必需的共享路径、兼容适配器和新集成测试；
- 可恢复的 scoped commit、按已批依赖顺序集成，以及 tracker/LATEST/changelog 回写。

## 执行约束

- 不以统一授权为理由降低 Locked/Frozen、AC/E4/E5、独立审计、确定性、信息边界或性能门禁。
- 不覆盖或清理来源不明的用户改动；优先通过 worktree/提交导出隔离。
- 系统/沙箱强制的外部权限不能被文档授权替代；如果 Task 19 完成确实必需，直接发起平台授权请求。凭据、外发布/push、删除和历史重写在非必需时仍跳过，不阻断其他工作。
- 只有当无安全替代且工作客观无法继续时，才将具体阻塞记入交付报告；不再请求“是否继续”类授权。
