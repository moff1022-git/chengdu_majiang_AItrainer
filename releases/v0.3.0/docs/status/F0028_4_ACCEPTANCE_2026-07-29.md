# F0028-4 人类化有限认知验收 — 2026-07-29

## 结论

**通过，F0028-4 状态 `Approved → Done`。**

已实现持续认知状态、公开信息有限记忆、稳定 Top-K 注意力、人格/水平差异、计划惯性与重启、满意停止、近似方案有界噪声及可复现思考时间。策略仍严格限定为 PlayerView v2 输入，动作全部来自 legal actions，未升级任何引擎或协议版本。

## 实现范围

| 能力 | 实现 |
|------|------|
| 记忆 | 可见 token、事件步衰减、显著强化、exact→fuzzy、稳定容量淘汰 |
| 注意力 | 全体对象显著度、稳定 Top-K、Top-K 内 softmax |
| 人格/水平 | novice/normal/skilled/expert 容量/阈值/噪声系数；三种 style 修正 |
| 持续计划 | 上一计划惯性、phase/公开事件/计划变化重启；只影响近似分差候选 |
| 满意停止 | 认知顺序前缀检查；阈值即停，否则 checked best |
| 有界噪声 | SHA-256 派生流；仅近似 ordinary；mandatory 零 RNG |
| 思考时间 | 稳定模型值、受 GP-022 上界限制、不 sleep |
| RP/trace | RP-024–029；内存 DecisionTrace v2 |

## 自动验收

| 门禁 | 结果 |
|------|------|
| humanlike 定向测试 | **52 passed** |
| 全量测试 | **332 passed / 1 skipped**（29.29s） |
| compileall / diff check | 通过 |
| 2/3/4 人批跑 | 各 50 局，共 150 局全部完成；零策略崩溃/非法动作 |
| 批跑墙钟 | 2 人 7.77s；3 人 12.88s；4 人 16.19s |
| 同 seed 双跑 | 50 局动作摘要一致：`45e521ee074f634378f9894022e15807683b26d72981f23547ec518815e9b185` |
| 跨 PYTHONHASHSEED | 1/777 各 10 局摘要一致：`ad3e5c62d589944cc5b2475310d499274861fe2f3daf573d69b03cddc0bb40a7` |
| 单决策性能 | 1000 次 p95 **1.1968 ms**，max 3.6212 ms（≤25 ms） |
| RuleAI 对照 | 20 局 humanlike 6.70s / RuleAI 2.68s = **2.50×**（≤3.5×） |

性能环境：macOS arm64，Python 3.12.13，项目 `.venv`。

## 认知运行抽样

20 局四人、4518 次决策、零崩溃：

- `mandatory=3457`、`satisficing=364`、`best_checked=697`；
- 116 次决策实际进入有界随机门；
- 检查候选数 1–6；
- 模型思考时间 307–1391 ms，均在 GP-022 上界内，运行时未 sleep。

## 隔离与兼容

- AST/集成测试继续确认 humanlike 生产模块无 `_engine_state`、`GameState` 或 `training.oracle`。
- 记忆只从 PlayerView payload 的公开弃牌、定缺、状态、副露和最近事件提取 token。
- 累计可见历史不会在每次决策中伪装成新提示；遗忘后仅重新出现的公开 token 可恢复精确记忆。
- APP 0.2.1 / state schema 5 / PlayerView 2 / persistence 1 / wire 1 均保持不变。
- `humanlike_v2` 仍为显式选配；rule_ai/current_s2 默认行为未改变。

## 已知限制

- CognitiveState 仅驻留当前玩家进程；中断恢复和持久化 RNG/记忆快照属于 F0028-5。
- Observation 的 event index 仍投影自 turn index；同 turn 多个公开变化由公开 token 差异推进逻辑认知步。
- 默认四座配置当前均为 normal/balanced；不同 level/style 的方向性由固定夹具验证，产品 profile 多样化需另行配置变更。
- 本轮未执行 GUI 人工体感测试；不影响自动功能门禁，后续可与 F0028-3 完整人工测试合并。

## 证据路径

- 规格：`docs/features/F0028_4_human_cognition.md`
- 实现：`players/humanlike/{memory,attention,cognition,policy,player}.py`
- 测试：`tests/humanlike_v2/test_cognition.py`
- 临时批跑：`/tmp/f0028_4_batch_*`、`/tmp/f0028_4_hash_*`、`/tmp/f0028_4_perf_*`（不作为跨机状态源）
